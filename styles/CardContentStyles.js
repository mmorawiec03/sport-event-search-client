import { StyleSheet } from 'react-native';

export const cardContentStyles = StyleSheet.create({
    center: {
        alignItems: 'center',
    },
    nameText: {
        fontWeight: 'bold',
        fontSize: 25,
        margin: 10
    },
    detailsText: {
        fontSize: 16,
        marginBottom: 8
    },
    labelText: {
        fontSize: 20,
        fontWeight: 'bold',
        marginBottom: 8
    }
});